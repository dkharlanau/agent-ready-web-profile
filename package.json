{
  "name": "agent-ready-web-profile",
  "version": "0.1.0",
  "private": true,
  "description": "Specification, schema and validator for the Agent-Ready Web Profile (ARWP).",
  "type": "module",
  "scripts": {
    "validate:examples": "node scripts/test.mjs",
    "test": "node scripts/test.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dkharlanau/agent-ready-web-profile.git"
  }
}
