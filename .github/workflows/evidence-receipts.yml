name: Evidence receipt integrity

on:
  push:
    branches: [main]
    paths:
      - 'lib/evidence-receipt.mjs'
      - 'bin/arwp-receipt.mjs'
      - 'schema/evidence-receipt.schema.json'
      - 'scripts/evidence-receipt-test.mjs'
      - 'scripts/evidence-receipt-cli-test.mjs'
      - 'docs/EVIDENCE-RECEIPTS.md'
      - 'package.json'
      - '.github/workflows/evidence-receipts.yml'
  pull_request:
    paths:
      - 'lib/evidence-receipt.mjs'
      - 'bin/arwp-receipt.mjs'
      - 'schema/evidence-receipt.schema.json'
      - 'scripts/evidence-receipt-test.mjs'
      - 'scripts/evidence-receipt-cli-test.mjs'
      - 'docs/EVIDENCE-RECEIPTS.md'
      - 'package.json'
      - '.github/workflows/evidence-receipts.yml'

permissions:
  contents: read

jobs:
  receipts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm
      - name: Install dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Validate evidence receipt model, schema and CLI
        run: npm run test:receipt
      - name: Smoke-test npm artifact with arwp-receipt included
        run: npm run test:package
