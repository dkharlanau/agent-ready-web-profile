# ARD registry scope guardrail

Future ARD registry search must be explicit and bounded. Default site resolution must not automatically federate across registries or recursively traverse nested catalogs. Any registry implementation needs request/referral/result caps, public-HTTPS validation, provenance on every returned resource and frozen-corpus decision-quality verification.
