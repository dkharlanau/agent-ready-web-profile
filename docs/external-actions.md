# External actions

The current ARWP dataset-publication increment intentionally stops before fabricating third-party state.

For CBT Cards and Metkagram, repository-side metadata preparation is active in `main`. The only remaining DOI-specific step is an owner-authorized archive publication. After an external archive issues the identifier, the repository should verify resolution and update canonical Dataset and citation metadata.
